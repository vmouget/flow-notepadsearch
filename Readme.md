Flow.Launcher.Plugin.NotepadSearch
==================

A plugin for the [Flow launcher](https://github.com/Flow-Launcher/Flow.Launcher).

Search in notepad **content** and jumps you to the matching window.

> **Note:** Windows only — the plugin reads live Notepad windows through the Win32 API.

### Usage

    n: <arguments>
